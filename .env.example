# ── Yaclam environment template (frontend placeholders → wire in backend) ──
NEXT_PUBLIC_API_URL=http://localhost:9000/api
# Payment methods (WaafiPay / Stripe) are loaded dynamically from the backend /payment/methods API.

